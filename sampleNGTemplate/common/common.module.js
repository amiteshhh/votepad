(function () {
    'use strict';
    /**
     * @ngdoc object
     * @name app.common
     *
     * @description
     * This is `app.common` module.
     */

    var moduleName = 'app.common',
            requires = [];

    angular.module(moduleName, requires);

})();