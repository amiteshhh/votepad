(function () {
    'use strict';
    /**
     * @ngdoc object
     * @name app.settings
     *
     * @description
     * This is `app.settings` module.
     */

    var moduleName = 'app.settings',
            requires = [];

    angular.module(moduleName, requires);

})();