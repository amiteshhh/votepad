(function () {
    'use strict';
    /**
     * @ngdoc object
     * @name app.profile
     *
     * @description
     * This is `app.profile` module.
     */

    var moduleName = 'app.profile',
            requires = [];

    angular.module(moduleName, requires);

})();