(function () {
    'use strict';
    /**
     * @ngdoc object
     * @name app.poll
     *
     * @description
     * This is `app.poll` module.
     */

    var moduleName = 'app.poll',
            requires = [];

    angular.module(moduleName, requires);

})();